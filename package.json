{
	"name": "@jtmdias/frontend",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"engines": {
		"node": ">= 20",
		"npm": ">= 10"
	},
	"scripts": {
		"all:build": "lerna run build --stream",
		"all:test": "lerna run test",
		"all:reinstall": "rimraf node_modules && npm install",
		"all:upgrade": "npx npm-check-updates",
		"version": "changeset version",
		"release": "changeset publish"
	},
	"devDependencies": {
		"@changesets/changelog-github": "^0.5.0",
		"@changesets/cli": "^2.27.1",
		"@types/node": "^20.11.20",
		"lerna": "^8.1.2",
		"rimraf": "^5.0.5",
		"typescript": "^5.3.3"
	}
}
